/**
 * Shim de compatibilidade — a fonte real vive em `src/lib/seo/siteUrl.ts`
 * (camada isomórfica reutilizada pelo Worker Edge Renderer).
 */
export { SITE_URL, PUBLIC_SITE_URL } from "@/lib/seo/siteUrl";
