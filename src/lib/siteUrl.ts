/**
 * Fonte ÚNICA da URL pública do projeto.
 *
 * Alterar aqui reflete automaticamente em:
 *  - canonical / og:url gerados por `SeoHead` e pelo wrapper `SEO`
 *  - fallback institucional de `og:image` / `twitter:image`
 *  - qualquer outra URL absoluta derivada de `SITE_URL`
 *
 * Não duplicar este valor em outros arquivos.
 */
export const SITE_URL = "https://convite-de-evento.lovable.app";

/** Alias explícito para uso em código que preferir o nome PUBLIC_SITE_URL. */
export const PUBLIC_SITE_URL = SITE_URL;
